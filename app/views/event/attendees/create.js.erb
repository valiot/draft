label.setObjectText("Name", '<%= @current_user.name.split(' ')[0] %>');
label.print(printerName);
var win = window.open('<%= event_main_url %>', '_blank');
win.focus();window.setTimeout(function(){this.close();},1000);
